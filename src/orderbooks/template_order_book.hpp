#pragma once

#include "../core/i_order_book.hpp"
#include <vector>

namespace hft
{

/**
 * @brief A template for students to implement their own order book algorithms.
 *
 * To use this:
 * 1. Copy this file to src/orderbooks/MyOrderBook.hpp
 * 2. Rename the class to MyOrderBook
 * 3. Implement the virtual methods below
 * 4. Add your book to src/core/OrderBookFactory.hpp
 */
class TemplateOrderBook : public IOrderBook
{
  public:
    TemplateOrderBook() = default;
    virtual ~TemplateOrderBook() = default;

    /**
     * @brief Adds a new order to the book.
     * If the order matches existing orders, it should generate trades.
     */
    void addOrder(const Order &order) override
    {
        // TODO: Implement your matching logic here
        // 1. Check if order can be matched against opposite side
        // 2. If matched, create Trade objects and store them
        // 3. If not fully matched, add remaining quantity to the book
    }

    /**
     * @brief Cancels an existing order by its ID.
     */
    void cancelOrder(OrderId orderId) override
    {
        // TODO: Find the order by ID and remove it from the book
    }

    /**
     * @brief Modifies the quantity of an existing order.
     */
    void modifyOrder(OrderId orderId, Quantity newQuantity) override
    {
        // TODO: Find the order and update its quantity
        // Note: In HFT, modifying quantity usually loses price-time priority
    }

    /**
     * @brief Returns trades generated by the last operation and clears them.
     */
    std::vector<Trade> match() override
    {
        // TODO: Return the list of trades generated since the last call
        return {};
    }

    /**
     * @brief Returns the total number of active orders in the book.
     */
    std::size_t getOrderCount() const override
    {
        // TODO: Return count of orders
        return 0;
    }

    /**
     * @brief Returns the current best bid price.
     */
    Price getBestBid() const override
    {
        // TODO: Return highest bid price or 0 if empty
        return 0;
    }

    /**
     * @brief Returns the current best ask price.
     */
    Price getBestAsk() const override
    {
        // TODO: Return lowest ask price or max if empty
        return std::numeric_limits<Price>::max();
    }
};

} // namespace hft
